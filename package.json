{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/calculadora-eletrica run dev",
    "build": "pnpm --filter @workspace/calculadora-eletrica run build",
    "typecheck": "pnpm --filter @workspace/calculadora-eletrica run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
